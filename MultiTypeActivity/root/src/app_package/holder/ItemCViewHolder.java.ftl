package ${packageName}.holder;

import android.view.View;
import ${packageName}.adapter.${adapterClass};
import ${packageName}.bean.ItemC;


public class ItemCViewHolder extends BaseViewHolder<ItemC> {

    public ItemCViewHolder(View itemView) {
        super(itemView);
    }

    @Override
    public void setUpView(ItemC model, int position, ${adapterClass} adapter) {

    }
}
